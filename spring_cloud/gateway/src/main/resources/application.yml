server:
  port: 9912

spring:
  application:
    name: kubo-gateway
  profiles:
    active: value,formal,private
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.2.5:8848
      config:
        import: optional:nacos:1
        server-addr: 192.168.2.5:8848
